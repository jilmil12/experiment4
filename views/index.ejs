<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blog Posts</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>📜 Blog Posts</h1>
  <div class="posts">
    <% if (posts.length > 0) { %>
      <% posts.forEach(post => { %>
        <div class="post">
          <h2><%= post.title %></h2>
          <p><%= post.content %></p>
          <small>By <%= post.author_name || 'Unknown' %> on <%= post.created_at.toLocaleString() %></small>
        </div>
      <% }) %>
    <% } else { %>
      <p>No posts available.</p>
    <% } %>
  </div>
</body>
</html>
